<% if review.product_url && allow_move_to_shop? %>
  <%= content_tag :a, class: 'link-to-product link-iframe', data: {url: review.product_url} do %>
    <%= image_tag review.product.image_url, alt: review.product_name, width: 80, height: 105, class: 'link-to-product' %>
  <% end %>
<% else %>
  <%= content_tag :span, class: 'link-to-product' do %>
    <%= image_tag review.product.image_url, alt: review.product_name, width: 80, height: 105 %>
  <% end %>
<% end %>

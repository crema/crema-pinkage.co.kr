<% return unless brand.review_show_score %>
<% score = review&.score unless score %>
<%= content_tag :div, class: "star-rating-container #{local_assigns[:klass]}" do %>
  <% (1..5).each do |i| %>
    <div class="pinkage_star">
      <%= content_tag :span, nil, class: "#{score && i > score ? 'sprites-pinkage-star-empty' : 'sprites-pinkage-star'}" %>
    </div>
  <% end %>
<% end %>
